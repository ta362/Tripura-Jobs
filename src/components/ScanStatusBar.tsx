import React from 'react';
import { useJobs } from '../context/JobContext';
import { Activity, AlertTriangle, CheckCircle2, Clock, RefreshCw } from 'lucide-react';

// Scan status bar removed per user request
